# Terminal: Dateien auflisten

Um alle Dateien im aktuellen Ordner als Liste anzuzeigen:

```bash
ls -la
```

- `-l` = long format, zeigt Details wie Rechte, Größe und Datum
- `-a` = all, zeigt versteckte Dateien (die mit einem Punkt beginnen, z.B. `.gitignore`)